`goc3_small.json` is powerio's own synthetic case, derived from the hand
checked test data in PowerIO.jl. Tests mutate this one small document to cover
named UID collisions, interleaved device classes, optional violation prices,
and reactive capability modes. No third party GOC3 data is distributed with
the repository or published crate.
