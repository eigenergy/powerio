//! Version metadata used by PowerIO authored auxiliary files.

/// The PowerIO release version key used by auxiliary output such as geographic
/// data and solver bundle manifests. PowerIO IR has its own `schema` and
/// `version` fields.
pub const VERSION_KEY: &str = "powerio_version";
