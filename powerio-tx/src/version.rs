//! Version metadata used by PowerIO authored auxiliary files.

/// The package version key used by auxiliary output such as geographic data
/// and solver bundle manifests. PowerIO IR has its own fixed `schema` and
/// `version` fields.
pub const VERSION_KEY: &str = "powerio_version";
