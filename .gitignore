/target
/out
/Cargo.lock.bak
*.swp
.DS_Store
.claude/

# Python bindings (maturin / PyO3). dist/ and build/ are anchored to the
# repo root: tests/data/dist holds fixtures, not build output.
*.so
*.pyd
/dist/
/build/
wheelhouse/
*.egg-info/
__pycache__/
.pytest_cache/
.venv/
.venv-validate/
.cache/
tests/data/large/

# The capi smoke binary writes its PyPSA folder next to the case it is given.
tests/data/*-pypsa-smoke/

# Scratch Julia project for the PMD oracle; its Manifest pins local paths.
powerio-dist/tools/pmd/Manifest.toml

# Machine-readable bench output (render_tables.py reads it; numbers are per-machine)
evals/validation/results/

# asv working dirs (built envs, raw results, generated dashboard)
evals/performance/asv/env/
evals/performance/asv/results/
evals/performance/asv/html/
tests/data/local_pwb_corpus.tsv
docs/book/
docs/src/releasing*.md
private/

# Playwright MCP browser session artifacts
.playwright-mcp/
