# powerio-core

`powerio-core` contains the dependency neutral types shared by PowerIO's
network, calculation, matrix, and facade crates. It owns retained input
sources, diagnostics and errors, `PioModule<T>`, time and scenario containers,
and output destinations. It contains no electrical network or parser.
