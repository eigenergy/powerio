name: Python

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  release:
    types: [ published ]
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      # `cargo clippy` at the root skips powerio-py (it's not in default-members),
      # so lint the extension explicitly with the same native features as wheels.
      - name: Clippy (powerio-py)
        run: cargo clippy -p powerio-py --no-deps --features extension-module,gridfm -- -D warnings

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: [ "3.9", "3.12" ]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      # `pip install .[all,mcp]` builds the extension via the maturin backend and
      # pulls scipy/numpy/networkx and Polars (matrix + graph + GridFM read tests)
      # plus the MCP SDK. The native gridfm writer is compiled in through
      # pyproject.toml. The `mcp` extra carries a
      # `python_version >= '3.10'` marker, so it installs the SDK on 3.12
      # (test_mcp runs) and resolves to nothing on 3.9 (test_mcp skips). Quote the
      # extras so the shell doesn't glob `[all,mcp]`.
      - name: Install powerio + test deps
        run: pip install '.[all,mcp]' pytest
      - name: Run tests
        run: pytest python/tests -q
      # The core import path: parse/convert/write import nothing but the
      # interpreter. The native gridfm writer is present in the wheel too; Polars
      # is needed only to read the Parquet output from Python.
      - name: Zero-dep parse path
        run: |
          python -m venv /tmp/zerodep
          /tmp/zerodep/bin/pip install .
          /tmp/zerodep/bin/python - <<'PY'
          from pathlib import Path
          import tempfile
          import sys, powerio
          c = powerio.parse_file("tests/data/case14.m")
          assert c.to_matpower() == open("tests/data/case14.m").read()
          assert "numpy" not in sys.modules and "scipy" not in sys.modules
          assert powerio._powerio._has_gridfm
          out = c.write_gridfm(tempfile.mkdtemp(prefix="powerio-gridfm-"))
          assert Path(out["dir"], "gridfm_meta.json").is_file()
          PY

  # Build the same wheel users get, install the GridFM and pandas compatibility
  # extras, and run the Parquet read/write tests before merge and on main.
  test-gridfm:
    name: test (gridfm extra)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install maturin
        run: pip install maturin
      # Install a wheel (not `develop`): a regular site-packages install wins the
      # `import powerio` race against the repo's `powerio/` crate directory, where
      # an editable install would not.
      - name: Build the wheel
        run: maturin build --release --out dist
      - name: Install wheel + test deps
        run: |
          wheel=$(ls dist/*.whl)
          pip install "${wheel}[all,gridfm,pandas]" pytest
      # `_has_gridfm` is now true, so the @gridfm_only tests run instead of skipping.
      - name: Run gridfm tests
        run: pytest python/tests -q -k gridfm

  wheels:
    name: wheels (${{ matrix.platform.target }} on ${{ matrix.platform.runner }})
    if: github.event_name == 'release' || github.event_name == 'workflow_dispatch'
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - { runner: ubuntu-latest,  target: x86_64 }
          - { runner: ubuntu-latest,  target: aarch64 }
          - { runner: macos-13,       target: x86_64 }
          - { runner: macos-14,       target: aarch64 }
          - { runner: windows-latest, target: x64 }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      # abi3-py39 → one wheel per platform covers Python 3.9+.
      - name: Build powerio wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          manylinux: auto
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.platform.runner }}-${{ matrix.platform.target }}
          path: dist

  sdist:
    if: github.event_name == 'release' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build powerio sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist
      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist

  publish:
    name: publish to PyPI
    if: github.event_name == 'release'
    needs: [ lint, test, test-gridfm, wheels, sdist ]
    runs-on: ubuntu-latest
    # Protect this environment with required reviewers before launch.
    environment: pypi
    permissions:
      id-token: write  # PyPI trusted publishing; no stored token
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Check distributions
        run: |
          python -m pip install twine
          python -m twine check dist/*
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
