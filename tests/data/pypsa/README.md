# PyPSA fixture

`example/` was generated by PyPSA 1.2.2 (`Network.export_to_csv_folder`) and is
committed byte exact as the tool wrote it, including `network.csv`,
`snapshots.csv`, `meta.json`, and `crs.json`. Three named buses, a line, a
transformer, three generators (one per control mode), a load, a storage unit, a
link, and a shunt:

```python
import pypsa

n = pypsa.Network(name="example")
n.add("Bus", "north", v_nom=110.0)
n.add("Bus", "south", v_nom=110.0)
n.add("Bus", "east", v_nom=20.0)
n.add("Line", "l1", bus0="north", bus1="south", r=0.5, x=2.0, b=1e-5, s_nom=100.0)
n.add("Transformer", "t1", bus0="south", bus1="east", r=0.01, x=0.1, s_nom=60.0, tap_ratio=1.05)
n.add("Generator", "g-slack", bus="north", control="Slack", p_nom=120.0, p_set=50.0,
      marginal_cost=12.0, marginal_cost_quadratic=0.04)
n.add("Generator", "g-pv", bus="south", control="PV", p_nom=80.0, p_set=30.0, marginal_cost=20.0)
n.add("Generator", "g-pq", bus="east", control="PQ", p_nom=10.0, p_set=5.0)
n.add("Load", "d1", bus="east", p_set=40.0, q_set=10.0)
n.add("StorageUnit", "s1", bus="south", p_nom=25.0, max_hours=4.0, p_set=3.0,
      state_of_charge_initial=20.0, efficiency_store=0.91, efficiency_dispatch=0.92)
n.add("Link", "hvdc1", bus0="north", bus1="south", p_nom=30.0, p_set=10.0, efficiency=0.97)
n.add("ShuntImpedance", "sh1", bus="south", g=0.0, b=1e-4)
n.export_to_csv_folder("tests/data/pypsa/example")
```

A genuine export carries no `powerio_base_mva` column, so the reader takes
base_mva = 1.0 for this fixture.
